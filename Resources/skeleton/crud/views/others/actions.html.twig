                {%- for action in record_actions %}
                        <a class="btn" href="{{ "{{ path('admin_"~ route_name_prefix ~"_"~ action ~"', { 'id': entity.id }) }}" }}">{{ action }}</a>
                {%- endfor %}
